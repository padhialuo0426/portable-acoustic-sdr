# 本地编辑器/工具配置
.claude/

# 操作系统产生的目录元数据（macOS Finder / Windows 资源管理器）
.DS_Store
Thumbs.db

# 构建产物
build/

# 运行/测试产物
tests/

# 运行/测试产物
*.mat
*.wav
*.raw
tx_truth.txt
# 但保留作为固定样例的数据(供离线试解码)
!**/sample_data/*.mat

# Simulink 相关：模型缓存与生成目录都不入库
**/slprj/
**/*.slxc
**/*.slx.r*
# Simulink 生成的代码整目录不入库：改完模型自己重新生成（见各实验文档
# 「重新生成模型」一节）。本实验宿主机必须装 MATLAB，不存在没有它的场景。
**/*_ert_rtw/
