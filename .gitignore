# 本地编辑器/工具配置
.claude/

# GUI 本地连接信息（含密码）
/common/connection.json

# 操作系统产生的目录元数据（macOS Finder / Windows 资源管理器）
.DS_Store
Thumbs.db

# 构建产物
build/

# 运行产物
*.mat
*.wav
*.raw
tx_truth.txt
# 但保留作为固定样例的数据(供离线试解码)
!**/sample_data/*.mat

# Simulink 相关：模型缓存与生成目录都不入库
**/slprj/
**/*.slxc
**/*.slx.r*
# Simulink 生成的代码整目录不入库：生成与部署流程见 documents/构建与部署.md。
**/*_ert_rtw/
